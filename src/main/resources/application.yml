spring:
  graphql:
    path: /jpmc-employees
  datasource:
    url: jdbc:postgresql://jpmcemployees.cq20blvqi5lj.us-east-1.rds.amazonaws.com:5432/postgres
    username: jpmcadmin
    password: nhan1276
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  main:
    allow-circular-references: true

server:
  port: 8080